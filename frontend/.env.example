# Copie este arquivo para ".env" (o Vite carrega ".env" automaticamente;
# não commite o ".env" em si, só este exemplo).

# URL do backend Spring Boot que este frontend vai consumir.
VITE_API_URL=http://localhost:8080
